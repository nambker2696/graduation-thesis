<%= link_to 'Home', root_url %> <i class="fa fa-chevron-right"></i> Checkout booking
<h2>Checkout</h2>
<div class="booking-form">
  <div class="stepwizard">
    <div class="stepwizard-row setup-panel">
      <div class="stepwizard-step">
        <a href="#step-1" type="button" class="btn btn-primary btn-circle">1</a>
        <p>Choose timeslot</p>
      </div>
      <div class="stepwizard-step">
        <a href="#step-2" type="button" class="btn btn-default btn-circle" disabled="disabled">2</a>
        <p>Choose seat</p>
      </div>
      <div class="stepwizard-step">
        <a href="#step-3" type="button" class="btn btn-default btn-circle" disabled="disabled">3</a>
        <p>Step 3</p>
      </div>
    </div>
  </div>
  <%= form_for(@booking) do |f| %>
  <% if @booking.errors.any? %>
  <div id="error_explanation">
    <div class="alert alert-danger">The form contains <%= pluralize(@booking.errors.count, "error") %>.</div>
    <ul>
      <% @booking.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
  <% end %>

  <div class="row setup-content" id="step-1">
    <div class="col-xs-6">
      <div class="col-md-12">
        <h3>Choose Timeslote</h3>
        <!-- datepicker -->
        <div class="well">
          <div id="datetimepicker2" class="input-append">
            <input data-format="MM/dd/yyyy HH:mm:ss PP" type="text"></input>
            <span class="add-on">
              <i data-time-icon="icon-time" data-date-icon="icon-calendar">
              </i>
            </span>
          </div>
        </div>





        <div class="form-group">
          <%= f.label :timeslot_id,class: "control-label" %>
          <%= f.text_field :timeslot_id, class: 'form-control' %>
        </div>
        <div class="form-group">        
          <%= f.label :day_to_eat,class: "control-label" %>
          <%= f.text_field :day_to_eat, class: 'form-control' %>
        </div>
        <button class="btn btn-primary nextBtn btn-lg pull-right" type="button" >Next</button>
      </div>
    </div>
  </div>

  <div class="row setup-content" id="step-2">
    <div class="col-xs-6">
      <div class="col-md-12">
        <h3> Chooe Seat</h3>
        <div class="form-group">
          <%= f.label :seat_id,class: "control-label" %>
          <%= f.text_field :seat_id,class: "form-control" %>
        </div>
        <button class="btn btn-primary nextBtn btn-lg pull-right" type="button" >Next</button>
      </div>
    </div>
  </div>
  <div class="row setup-content" id="step-3">
    <div class="col-xs-6 col-md-offset-3">
      <div class="col-md-12">
        <h3> Checkout </h3>
        <div class="form-group">
          <%= f.label :delivery_type,class: "control-label" %>
          <%= f.select :delivery_type,Booking::DELIVERY_TYPES, prompt: 'Select a delivery method', class: 'form-control' %>
        </div>
        <div class="form-group">
          <%= f.label :payment_type,class: "control-label" %>
          <%= f.select :payment_type, Booking::PAYMENT_TYPES, prompt: 'Select a payment method', class: 'form-control' %>
        </div>
        <%= f.hidden_field :user_id, value: current_user.id %>

        <div class="form-group">
          <%= f.label :location_id,class: "control-label" %>
          <%= f.text_field :location_id, class: 'form-control' %>
        </div>
        
        <div class="form-group">
          <%= f.label :reservation_place,class: "control-label" %>
          <%= f.text_field :reservation_place, class: 'form-control' %>
        </div>
        <%= f.submit yield(:button_text), class: "btn btn-success btn-lg pull-right" %>
      </div>
    </div>
  </div>
  <% end %>
</div>
</div>


<script>
  $(document).ready(function () {
    var navListItems = $('div.setup-panel div a'),
    allWells = $('.setup-content'),
    allNextBtn = $('.nextBtn');

    allWells.hide();

    navListItems.click(function (e) {
      e.preventDefault();
      var $target = $($(this).attr('href')),
      $item = $(this);

      if (!$item.hasClass('disabled')) {
        navListItems.removeClass('btn-primary').addClass('btn-default');
        $item.addClass('btn-primary');
        allWells.hide();
        $target.show();
        $target.find('input:eq(0)').focus();
      }
    });

    allNextBtn.click(function(){
      var curStep = $(this).closest(".setup-content"),
      curStepBtn = curStep.attr("id"),
      nextStepWizard = $('div.setup-panel div a[href="#' + curStepBtn + '"]').parent().next().children("a"),
      curInputs = curStep.find("input[type='text'],input[type='url']"),
      isValid = true;

      $(".form-group").removeClass("has-error");
      for(var i=0; i<curInputs.length; i++){
        if (!curInputs[i].validity.valid){
          isValid = false;
          $(curInputs[i]).closest(".form-group").addClass("has-error");
        }
      }

      if (isValid)
        nextStepWizard.removeAttr('disabled').trigger('click');
    });

    $('div.setup-panel div a.btn-primary').trigger('click');
  });


  $(function() {
    $('#datetimepicker2').datetimepicker({
      language: 'en',
      pick12HourFormat: true
    });
  });
</script>